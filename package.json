{
  "name": "iphone-label-printer-tauri",
  "private": true,
  "version": "1.0.15",
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "test": "npm run test:frontend && npm run test:catalog",
    "test:frontend": "tsx --test tests/*.test.ts",
    "test:catalog": "node --test scripts/*.test.mjs",
    "validate:catalog": "node scripts/validate-catalog.mjs",
    "refresh:catalog": "node scripts/update-catalog.mjs",
    "check:catalog-refresh": "node scripts/update-catalog.mjs --check",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-process": "^2.3.1",
    "@tauri-apps/plugin-updater": "^2.10.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "tsx": "^4.23.12",
    "typescript": "~7.0.2",
    "vite": "^8.1.5"
  }
}
